---
title: "UniPty v0.2.1：新增 zigpty Backend"
date: 2026-09-07T12:21:53Z
description: UniPty v0.2.1 新增官方 Backend 包 @unipty/backend-zigpty，基于 Zig 实现的零依赖 PTY 引擎 zigpty。本文对比它与 node-pty 路由的优缺点，以及使用前的注意事项。
author: jixoai
tags: [release, unipty]
repo: unipty
version: "0.2.1"
lang: zh
---

```text
$ npm view @unipty/backend-zigpty version
0.2.0
```

UniPty v0.2.1 发布了（GitHub Release 于 2026-09-07 12:21 UTC 切出）。这一版新增一个官方 Backend 包 `@unipty/backend-zigpty`，底层是 [zigpty](https://github.com/pithings/zigpty)（npm `zigpty@0.2.1`，MIT，pi0 维护），一个用 Zig 实现的 PTY 引擎。Node 侧从此有两个引擎可选，另一个是既有的 `@unipty/backend-node-pty`。

## zigpty 与 node-pty 的对比

两条路由的公共契约一致，差异都在引擎层。选型时主要看下面这张表。

| 维度 | zigpty | node-pty（@lydell 发行版） |
| --- | --- | --- |
| 运行时依赖 | 0 | 平台子包（optionalDependencies） |
| 安装脚本 | 无 | 无 |
| 分发方式 | 8 个平台元组的预编译全部打进主 tarball，约 420KB | 每个平台一个子包，按平台各装一份 |
| 安装健壮性 | `--omit=optional` 等安装姿势不影响 | optional 依赖被跳过时装不上 |
| 原生文本输出 | 有（`encoding: "utf8"`） | 有（`encoding: "utf8"`） |
| 字节写入 | 默认拒绝，需 `writeDecode` 选项 | 默认接受 |
| Windows | 拒绝就绪（见下文） | 声明支持，证据门控 |
| 传输读错误上报 | 不可区分（见下文） | 上报 `unsupported` |

zigpty 的优势集中在分发：没有运行时依赖、没有平台子包、预编译全在主包里，装什么就是什么，不依赖安装器对 optional 依赖的处理。对安装环境不可控的场景（受限 CI、离线镜像）更稳。

它的代价在后文「注意事项」：字节写入要多开一个选项，Windows 暂不可用。如果你的场景落在这两条上，node-pty 路由仍是更直接的选择。

## 使用

```bash
npm install unipty @unipty/backend-zigpty
```

```ts
import { UniPty } from "unipty";
import { createZigptyBackend } from "@unipty/backend-zigpty";

const backend = await createZigptyBackend();
const unipty = new UniPty({ backend });

const pty = unipty.spawn(["/bin/sh", "-i"], {
  terminal: { cols: 120, rows: 40 },
});
for await (const text of pty.stream({ encoding: "utf8" })) {
  process.stdout.write(text);
}
```

`spawn`、`stream`、`write`、`resize`、`close`、`terminate`、`exited` 的语义与另外三条路由完全一致。25 个场景的公共契约套件对安装后的包产物全量跑过，darwin-arm64 本地一份原生证据，CI 的 ubuntu 与 macos 各一份，已进[发布目录](https://unipty.jixoai.com/compatibility)。

## 注意事项

完整差异见 [README 的能力差异矩阵](https://github.com/jixoai/unipty/blob/v0.2.1/README.md#capability-differences-what-the-engines-actually-give-you)。下面四条需要提前知道。

1. **字节写入需要选项。** 引擎的 `write` 只收字符串，Endpoint 默认拒绝字节输入并给 `unsupported`。字节输入流（xterm.js 前端这类）开 `writeDecode` 即可。解码有状态、跨 chunk 安全。

```ts
const backend = await createZigptyBackend({ writeDecode: true });
pty.write(new TextEncoder().encode("echo hi\r"));
```

2. **Windows 拒绝就绪。** 引擎的 `pause()` / `resume()` 在 win32 上是空操作，输出背压无法传导。适配器因此在 Windows 上直接报 `unsupported`，元数据 targets 收窄到 darwin 与 linux。

3. **传输读错误不可区分。** 引擎不暴露传输层事件，适配器在退出窗口接管读流、用真实的 `end` / `close` 当 EOF，另有一个 50ms 的静默窗兜底。读错误与干净 EOF 在这条路由上无法区分，这是如实声明的限制。

4. **退出观察的形状。** 信号致死报 `{ exitCode: 0, signal: "SIGTERM" }`（退出码保持引擎原值）；可执行文件缺失报 `{ exitCode: 1, signal: null }`，是退出观察不是异常。

<details>
<summary>四条路由的信号致死报告对照</summary>

```text
node-pty   { exitCode: null, signal: "SIGTERM" }
zigpty     { exitCode: 0,    signal: "SIGTERM" }
bun        { exitCode: null, signal: "SIGTERM" }
deno-ffi   { exitCode: 1,    signal: null }
```

</details>

## 其余变更

- 修复 ubuntu 上快退子进程输出丢失：适配器改为在退出窗口接管读流（[3d56f71](https://github.com/jixoai/unipty/commit/3d56f71)）
- 修复 writeDecode 饱和拒绝推进解码器状态的问题（[f083127](https://github.com/jixoai/unipty/commit/f083127)）
- 发布目录注册表按路由身份键控并做双重身份校验（[b878ce4](https://github.com/jixoai/unipty/commit/b878ce4)、[f083127](https://github.com/jixoai/unipty/commit/f083127)）
- 文档全仓同步新路由（[021a82d](https://github.com/jixoai/unipty/commit/021a82d)）

完整清单见 [compare v0.2.0...v0.2.1](https://github.com/jixoai/unipty/compare/v0.2.0...v0.2.1)。

## 升级

无破坏性变更，纯新增包。Backend 包在 npm 上的版本是 0.2.0。

```bash
npm i @unipty/backend-zigpty@0.2.0
```

已经在用 `unipty@0.2.0` 的话，Core 不用动。

## 致谢

路由基于 [zigpty](https://github.com/pithings/zigpty) 构建并精确固定 0.2.1，感谢 pi0 与 UnJS 社区。

## 链接

- Changelog：[GitHub Release v0.2.1](https://github.com/jixoai/unipty/releases/tag/v0.2.1) · [compare v0.2.0...v0.2.1](https://github.com/jixoai/unipty/compare/v0.2.0...v0.2.1)
- 文档：[unipty.jixoai.com](https://unipty.jixoai.com) · [能力差异矩阵](https://github.com/jixoai/unipty/blob/v0.2.1/README.md#capability-differences-what-the-engines-actually-give-you) · [@unipty/backend-zigpty README](https://github.com/jixoai/unipty/blob/v0.2.1/packages/backend-zigpty/README.md)
- 升级：无破坏，一行 `npm i`
- 讨论：[GitHub Issues](https://github.com/jixoai/unipty/issues)
- npm：[@unipty/backend-zigpty](https://www.npmjs.com/package/@unipty/backend-zigpty) · [unipty](https://www.npmjs.com/package/unipty)
- 上一版：[UniPty v0.2.0：第一阶段完整收官](/blog/2026-09-06-unipty-v0-2-0/)
- English mirror: [/blog/2026-09-07-unipty-v0-2-1-en/](/blog/2026-09-07-unipty-v0-2-1-en/)
