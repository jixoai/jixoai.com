---
title: "UniPty v0.2.2：zigpty 路由从新增到 Windows 可用"
date: 2026-09-07T20:53:48Z
description: UniPty 在 v0.2.1 加入第四条官方路由 zigpty（零依赖、预编译随包分发），v0.2.2 撤掉其 Windows 门禁（适配层缓冲语义）并新增 outputSpool 磁盘化输出选项。一篇文章讲完这条路由的引入与第一次演进。
author: jixoai
tags: [release, unipty]
repo: unipty
version: "0.2.2"
lang: zh
---

```text
$ npm view @unipty/backend-zigpty version
0.2.1
```

zigpty 路由今天（2026-09-07）走了两步。中午的 v0.2.1（2026-09-07 12:21 UTC）把它加进官方路由集，Node 侧第一次可以在 node-pty 之外换引擎；晚间的 v0.2.2（2026-09-07 20:53 UTC）撤掉它在 Windows 上的就绪门禁，并给输出补了一个磁盘化缓冲。两版合起来只动一个新包，Core 与另外三条路由零改动。本文把两步放在一起讲。

## 为什么是 zigpty

底层引擎是 [zigpty](https://github.com/pithings/zigpty)（npm `zigpty@0.2.1`，MIT，pi0 维护），用 Zig 实现跨平台 PTY，经 NAPI 绑定暴露给 Node。选它做第四条路由，看的是分发模型。两条 Node 路由的公共契约一致，差异都在引擎层，选型时主要看下面这张表。

| 维度 | zigpty | node-pty（@lydell 发行版） |
| --- | --- | --- |
| 运行时依赖 | 0 | 平台子包（optionalDependencies） |
| 安装脚本 | 无 | 无 |
| 分发方式 | 8 个平台元组的预编译全部打进主 tarball，约 420KB | 每个平台一个子包，按平台各装一份 |
| 安装健壮性 | `--omit=optional` 等安装姿势不影响 | optional 依赖被跳过时装不上 |
| 原生文本输出 | 有（`encoding: "utf8"`） | 有（`encoding: "utf8"`） |
| 字节写入 | 默认拒绝，需 `writeDecode` 选项 | 默认接受 |
| Windows | 可运行，输出为缓冲式（v0.2.2 起，见下文） | 声明支持，证据门控 |
| 传输读错误上报 | 不可区分（见下文） | 上报 `unsupported` |

zigpty 的优势集中在分发：没有运行时依赖、没有平台子包、预编译全在主包里，装什么就是什么，不依赖安装器对 optional 依赖的处理。对安装环境不可控的场景（受限 CI、离线镜像）更稳。代价在字节写入要多开一个选项，以及 Windows 上的缓冲式输出，后文分别展开。

上游还有一处要当心的行为：预编译加载失败时，zigpty 库会静默回退到基于管道的伪 PTY，而 isatty 与内核几何尺寸在伪 PTY 上都不成立。适配器在就绪阶段硬门禁这个回退，`hasNative` 不真就直接给 `unsupported`，绝不拿假 PTY 冒充真的。

## 使用

```bash
npm install unipty @unipty/backend-zigpty
```

```ts
import { UniPty } from "unipty";
import { createZigptyBackend } from "@unipty/backend-zigpty";

const backend = await createZigptyBackend();
const unipty = new UniPty({ backend });

const pty = unipty.spawn(["/bin/sh", "-i"], {
  terminal: { cols: 120, rows: 40 },
});
for await (const text of pty.stream({ encoding: "utf8" })) {
  process.stdout.write(text);
}
```

`spawn`、`stream`、`write`、`resize`、`close`、`terminate`、`exited` 的语义与另外三条路由完全一致，换引擎不换语义。25 个场景的公共契约套件对安装后的包产物全量跑过，两次发布各过一遍，darwin-arm64 本地一份原生证据，CI 的 ubuntu 与 macos 各一份，已进[发布目录](https://unipty.jixoai.com/compatibility)。

## Windows：从门禁到适配

v0.2.1 引入这条路由时，Windows 上是失败关闭的：引擎的 ConPTY 预编译齐全，但其 Windows 实现的 `pause()` / `resume()` 是空方法（zigpty 0.2.1 的 `WindowsPty`），消费端驱动的输出背压传导不到内核。适配器因此拒绝就绪，而不是带着一个会无限增长的队列运行。当时的解除条件写的是"等上游补上流控"。

v0.2.2 不等了。UniPty 约束不了所有引擎，第三方开发者会有自己的 backend，用适配层弥合引擎差异本来就是这套架构的重点工作。门禁撤掉后，路由在 Windows 上照常运行：适配器持续排空引擎回调，代价如实声明为"输出背压在该平台传导不到内核"，与 Deno 路由对内部读线程的处理是同一类声明式限制。安装与启动不需要任何平台分支：

```ts
const backend = await createZigptyBackend(); // Windows 与 unix 同一行
```

支持声明仍由证据门控。Windows 元组在拿到公开契约证据之前保持 declared-unverified，[兼容性目录](https://unipty.jixoai.com/compatibility)如实呈现，门禁的撤除不等于验证的支持。

## outputSpool：磁盘化的输出缓冲

停读的消费者是内存的主要风险。终端挂到浏览器标签页上，用户切走十分钟，引擎在这十分钟里产出的输出没有去处，只能堆在适配器的队列里。Windows 之外这个问题同样存在，只是 unix 上内核背压会把压力还给子进程，堆不快。

`outputSpool` 把这个队列换成磁盘化的 FIFO：

```ts
const backend = await createZigptyBackend({
  outputSpool: { memoryBytes: 4 * 1024 * 1024 }, // 或直接 true 用默认值
});
```

机制是三段。内存头默认 1 MiB 封顶；越界的记录溢写到适配器自有的临时文件；回放严格跟随消费端的拉动节奏，流的水位线之外不多塞一条记录。公开流的字节与不开时完全一致：文本记录按完整记录往返，chunk 边界保持不变。子进程退出时，完成信号会等积压排空才发出，快退输出的尾巴不会被 EOF 截断；显式 `close()` 仍然同步完成并删除临时文件。

三个使用前需要知道的边界。溢写是同步 IO；积压期间的磁盘占用按设计不设上限，只受子进程自身输出量约束；进程被强杀时临时文件交由系统临时目录清理。在引擎能够暂停的平台上（unix），越过内存界还会把压力传导回内核，阻塞子进程而不是继续堆积。

## 注意事项

完整差异见 [README 的能力差异矩阵](https://github.com/jixoai/unipty/blob/v0.2.2/README.md#capability-differences-what-the-engines-actually-give-you)。除了上面两节，还有三条需要提前知道。

1. **字节写入需要选项。** 引擎的 `write` 只收字符串，Endpoint 默认拒绝字节输入并给 `unsupported`。字节输入流（xterm.js 前端这类）开 `writeDecode` 即可。解码有状态、跨 chunk 安全。

```ts
const backend = await createZigptyBackend({ writeDecode: true });
pty.write(new TextEncoder().encode("echo hi\r"));
```

2. **传输读错误不可区分。** 引擎不暴露传输层事件，适配器在退出窗口接管读流、用真实的 `end` / `close` 当 EOF，另有一个 50ms 的静默窗兜底。读错误与干净 EOF 在这条路由上无法区分，这是如实声明的限制。

3. **退出观察的形状。** 信号致死报 `{ exitCode: 0, signal: "SIGTERM" }`（退出码保持引擎原值）；可执行文件缺失报 `{ exitCode: 1, signal: null }`，是退出观察不是异常。

<details>
<summary>四条路由的信号致死报告对照</summary>

```text
node-pty   { exitCode: null, signal: "SIGTERM" }
zigpty     { exitCode: 0,    signal: "SIGTERM" }
bun        { exitCode: null, signal: "SIGTERM" }
deno-ffi   { exitCode: 1,    signal: null }
```

</details>

## 其余变更

**v0.2.2**

- 修复被暂停读取的洪水子进程收不到终止确认：引擎在积压未排空时会推迟退出观察，`terminate()` 现在在 kill 后恢复读取（[9bac5d7](https://github.com/jixoai/unipty/commit/9bac5d7)）
- 修复 `close()` 后读取暂停导致的同类悬挂，关闭后数据走丢弃路径（[9bac5d7](https://github.com/jixoai/unipty/commit/9bac5d7)）
- 修复背压暂停恰发生在退出前时内核滞留输出丢失：退出窗口内接管读流并显式恢复（[0a9a172](https://github.com/jixoai/unipty/commit/0a9a172)）
- 边界测试电池覆盖 memoryBytes 极值、空记录、损坏记录、积压取消等场景，包测试增至 70 项（[f8ad01d](https://github.com/jixoai/unipty/commit/f8ad01d)）

**v0.2.1**

- 修复 ubuntu 上快退子进程输出丢失：适配器改为在退出窗口接管读流（[3d56f71](https://github.com/jixoai/unipty/commit/3d56f71)）
- 修复 writeDecode 饱和拒绝推进解码器状态的问题（[f083127](https://github.com/jixoai/unipty/commit/f083127)）
- 发布目录注册表按路由身份键控并做双重身份校验（[b878ce4](https://github.com/jixoai/unipty/commit/b878ce4)、[f083127](https://github.com/jixoai/unipty/commit/f083127)）
- 文档全仓同步新路由（[021a82d](https://github.com/jixoai/unipty/commit/021a82d)）

完整清单见 [compare v0.2.1...v0.2.2](https://github.com/jixoai/unipty/compare/v0.2.1...v0.2.2) 与 [compare v0.2.0...v0.2.1](https://github.com/jixoai/unipty/compare/v0.2.0...v0.2.1)。

## 升级

npm 上的包版本比 GitHub tag 落后一位（tag v0.2.1 发布的包是 0.2.0，tag v0.2.2 发布的包是 0.2.1），安装时以 npm 版本为准：

```bash
npm i @unipty/backend-zigpty@0.2.1
```

`outputSpool` 默认关闭，不开则行为与 0.2.0 逐字节一致。已经在用 `unipty@0.2.0` 的话，Core 不用动。

## 致谢

路由基于 [zigpty](https://github.com/pithings/zigpty) 构建并精确固定 0.2.1，感谢 pi0 与 UnJS 社区。v0.2.2 发布前另有两轮独立复核，第一轮提出的三个阻塞问题全部修复后二轮通过。

## 链接

- Changelog：[GitHub Release v0.2.2](https://github.com/jixoai/unipty/releases/tag/v0.2.2) · [GitHub Release v0.2.1](https://github.com/jixoai/unipty/releases/tag/v0.2.1)
- 文档：[unipty.jixoai.com](https://unipty.jixoai.com) · [能力差异矩阵](https://github.com/jixoai/unipty/blob/v0.2.2/README.md#capability-differences-what-the-engines-actually-give-you) · [outputSpool 选项文档](https://github.com/jixoai/unipty/blob/v0.2.2/packages/backend-zigpty/README.md)
- 升级：见本文「升级」节
- 讨论：[GitHub Issues](https://github.com/jixoai/unipty/issues)
- npm：[@unipty/backend-zigpty](https://www.npmjs.com/package/@unipty/backend-zigpty) · [unipty](https://www.npmjs.com/package/unipty)
- 上一版：[UniPty v0.2.0：第一阶段完整收官](/blog/2026-09-06-unipty-v0-2-0/)
- English mirror: [/blog/2026-09-07-unipty-v0-2-2-en/](/blog/2026-09-07-unipty-v0-2-2-en/)
