---
title: "OpenDWeb v0.4.2：逻辑网络，不是系统级 VPN"
date: 2026-09-06T08:04:02Z
description: opendweb 仓库的第一份 GitHub Release：六个 npm 包各就各位（CLI 0.4.2 + SDK + Cloudflare Tunnel 插件 1.0.3），QUIC 直连优先、可自托管 Relay 回退的应用级组网，像游戏房间一样邀请制；从下一 tag 起 release notes 全自动。
author: jixoai
tags: [release, opendweb]
repo: opendweb
version: "0.4.2"
lang: zh
---

```text
$ curl http://localhost:8787/healthz
200
```

OpenDWeb v0.4.2 发布了（2026-09-06，[jixoai/opendweb](https://github.com/jixoai/opendweb) 仓库的第一份 GitHub Release）。这份 release 记录的是整个 npm 包家族的已发布状态；从下一个 tag 起，[release workflow](https://github.com/jixoai/opendweb/blob/main/.github/workflows/release.yml) 的自动 release notes 接管。

## 背景：多设备应用要的是房间，不是隧道

多设备应用需要组网时，现有的答案要么是系统级 VPN，权限大得吓人，覆盖全部设备的全部流量；要么是每个应用自己从零造一遍。OpenDWeb（opendweb）主张第三条路：**应用级逻辑网络**，成员像加入游戏房间一样走受控邀请，四层各就各位：

```text
身份层   Ed25519 EndpointId（稳定身份，与网络地址解耦；展示串 z-base-32）
名册层   Roster = 签名事实（Genesis/Grant/Join/Revoke），BLAKE3 内容寻址
         + union-merge 收敛；受控邀请 = issuer-online 单次兑换
         （challenge-response PoP + invite_id CAS 消费）
会话层   iroh 1.1：QUIC 直连 + NAT 穿透 + 自托管 relay 回退；
         双 ALPN（常规/redeem）；两侧门控（先门控后数据）；帧资源上限
同步层   不透明 envelope 双向收发（Automerge 适配器为后续独立 change）
```

邀请必须**在邀请方在线时**兑换（issuer-online 单次兑换）：邀请方的进程（例如一个 `chat` 会话）要在兑换期间保持运行。加入失败带稳定错误码（`error[join/<code>]`）。`NO_REACHABLE_PATH` 立即失败并给出指引，`DIAL_TIMEOUT` 提示邀请方可能已离线。

## 六个 npm 包，一个家族

| npm 包 | 版本 | 角色 |
| --- | --- | --- |
| [`opendweb`](https://www.npmjs.com/package/opendweb) | 0.4.2 | 服务端 CLI：`npx opendweb server` 起自托管 gateway（8787）+ relay（3340）；插件市场宿主 |
| [`@jixo/opendweb-server-binary`](https://www.npmjs.com/package/@jixo/opendweb-server-binary) | 0.3.2 | CLI 使用的服务端二进制包装；另暴露可编程 `startServer()` |
| [`@jixo/opendweb-client-sdk`](https://www.npmjs.com/package/@jixo/opendweb-client-sdk) | 0.3.2 | 在你自己的应用里嵌入 fabric 的 Node SDK（napi-rs；darwin-arm64 / win32-x64） |
| [`@jixo/opendweb-example`](https://www.npmjs.com/package/@jixo/opendweb-example) | 0.3.2 | 双进程参考客户端 CLI（`init` / `invite` / `join` / `chat`） |
| [`@jixo/opendweb-config`](https://www.npmjs.com/package/@jixo/opendweb-config) | 0.1.0 | 本地插件文件的 `definePlugin` helper（运行时无关：deno / bun / node） |
| [`@jixo/opendweb-ext-cf`](https://www.npmjs.com/package/@jixo/opendweb-ext-cf) | 1.0.3 | Cloudflare Tunnel 插件：API 推送 ingress、DNS 路由与端到端校验，可选 cloudflared 共生 |

Windows 工件（`dweb-server` exe + napi dll）由 tag workflow 现场构建（mingw 交叉编译），直接装进 npm tarballs，不需要单独下载。

## 自托管一条命令，客户端一个入口

服务端一条命令（或一个容器）。客户端只需要**一个**配置入口，gateway 经 `/services.json` 自动发现 relay，客户端永远不必知道 3340 端口的存在：

```bash
# 1. 起服务端（gateway + relay）
npx opendweb server
#   或：docker run -p 8787:8787 -p 3340:3340 ghcr.io/jixoai/opendweb:0.4.2

# 2. 每台客户端一次性配置（持久化到 ~/.opendweb/config.json）
npx @jixo/opendweb-example config set relay http://192.168.2.13:8787

# 3. 终端 A：初始化并保持 chat 会话（签发邀请）
npx @jixo/opendweb-example init --data ~/.dweb-a
npx @jixo/opendweb-example invite --data ~/.dweb-a --ttl 30m   # 复制令牌
npx @jixo/opendweb-example chat --data ~/.dweb-a

# 4. 终端 B（另一目录/设备）：兑换邀请并聊天
npx @jixo/opendweb-example join --data ~/.dweb-b <token>
npx @jixo/opendweb-example chat --data ~/.dweb-b
```

验证一个运行中的服务端：`curl http://localhost:8787/healthz` 得 200；`curl http://localhost:8787/services.json` 得服务清单。端到端回归有手册：[EXAMPLE.md](https://github.com/jixoai/opendweb/blob/main/EXAMPLE.md)（[中文版](https://github.com/jixoai/opendweb/blob/main/EXAMPLE-zh.md)），每次发布后照走一遍。

## 0.4.x 线的两个亮点

- **插件安装显式钉住 `@latest`**（[v1.0.1-cli](https://github.com/jixoai/opendweb/releases/tag/v1.0.1-cli) 的内容）：祖先目录里一份带过期依赖范围的 `package.json`，再也不能劫持版本解析。
- **ext-cf 1.0 的发现驱动重写**：浏览器登录或一个 API token 即可接入，控制面有 SDK 背书，cloudflared 可托管，token 粘贴 UX 支持整块或单条；经过一轮 Codex 复核整改（SDK 叶子导入、config-merge PUT、OAuth 持久化契约、安装路径与竞态修复）。更早的 `v0.4.0` 把连接器凭据与管理凭据拆开（一次线上 401 的修复）。

## 升级

```bash
npx opendweb@0.4.2 server          # 或：npm i -g opendweb@0.4.2
npm i @jixo/opendweb-client-sdk@0.3.2
docker run -d -p 8787:8787 -p 3340:3340 ghcr.io/jixoai/opendweb:0.4.2
```

镜像命名空间的历史一笔带过：`ghcr.io/jixoai/opendweb` 承载仓库改名 `jixoai/opendweb` 之前的全部 tag；改名后的构建发布在 `ghcr.io/jixoai/opendweb`。

## 致谢

会话层站在 [iroh](https://iroh.computer) 1.1 之上，QUIC 直连、NAT 穿透与自托管 relay 回退都是它的馈赠；SDK 的胶水来自 napi-rs。

## 链接

- Changelog：[GitHub Release v0.4.2](https://github.com/jixoai/opendweb/releases/tag/v0.4.2)（本仓库首份，此后自动化）
- 文档：[opendweb.jixoai.com](https://opendweb.jixoai.com) · [README](https://github.com/jixoai/opendweb/blob/v0.4.2/README.md) · [中文 README](https://github.com/jixoai/opendweb/blob/v0.4.2/README-zh.md) · [端到端手册 EXAMPLE.md](https://github.com/jixoai/opendweb/blob/main/EXAMPLE.md) · [中文版](https://github.com/jixoai/opendweb/blob/main/EXAMPLE-zh.md)
- 升级：见本文「升级」节（npx / npm i / docker 三选一）
- 讨论：[GitHub Issues](https://github.com/jixoai/opendweb/issues)
- 本站系列：[OpenIWeb v0.1.0](/blog/2026-09-06-openiweb-v0-1-0/) · [OpenTray v0.21.1](/blog/2026-09-06-opentray-v0-21-1/)
- English mirror: [/blog/2026-09-06-opendweb-v0-4-2-en/](/blog/2026-09-06-opendweb-v0-4-2-en/)
