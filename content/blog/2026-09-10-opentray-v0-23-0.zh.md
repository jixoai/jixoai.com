---
title: "OpenTray v0.23.0：把任意网址变成托盘应用"
date: 2026-09-10T12:44:27Z
description: OpenTray v0.23.0 起，create 可以收一个网址而不是一条命令，把任意页面打包成常驻托盘的窗口；标题、favicon 与 App 图标都从页面抓取或自动生成。
author: jixoai
tags: [opentray]
repo: opentray
version: "0.23.0"
lang: zh
---

```text
$ npm view create-opentray version
0.23.0
```

OpenTray v0.23.0 发布了（2026-09-10）。对使用者来说有两处变化：`create` 能收一个网址，不必再给一条命令；App 图标在你不提供的时候会自己生成。

## 网址代替命令

`create --url` 生成的托盘应用，窗口直接开在这个地址上。产物里没有 PTY、没有 shell 资源、也不依赖 `node-pty`，应用身份从地址推导。

```bash
npx create-opentray create --url https://example.com
# https://example.com/app → app id "app.com.example"，名字 "App"
```

地址在动手之前就已知，所以创建时会抓一次页面，把 `<title>` 和最佳 favicon 作为默认值。显式传入的 flag 永远优先；抓取失败就静默回落到地址推导出的名字和字形图标；`--no-scrape` 关掉这次抓取。

| | `--exec` 应用 | `--url` 应用 |
|---|---|---|
| 跑什么 | 你的命令，经 PTY 监管 | 不跑东西，窗口开在地址上 |
| 身份来源 | runner 加包名 | 网址 |
| 标题与图标默认值 | 你自己给 | 从页面抓一次 |

加 `--toolbar` 会套一层地址栏外壳，带后退、前进与重载按钮，以及焦点在壳内时可用的 ⌘/Ctrl+←→、⌘/Ctrl+[ 、⌘/Ctrl+R、F5、⌘/Ctrl+L 快捷键。禁止被嵌入的页面套不了这层壳：创建时会在抓取阶段读响应头（`X-Frame-Options` 与 CSP `frame-ancestors`），对方拒绝就回退成直连窗口并打印提示。不管用不用 toolbar，URL 应用的托盘菜单里都有 Reload。

## App 图标不用你画

省略 `appIcon` 时，运行时会用应用名的首字母合成一个字形图标写进 macOS 应用包，缓存在运行时目录下，Dock 上不再出现那个通用的可执行占位图。`appBundle.defaultAppIcon: false` 退回旧行为，显式传入的 `appIcon` 始终优先。

```ts
await createTray({ /* 托盘图标与菜单 */ }, {
  appId: "com.example.build",
  appName: "Build",
  appBundle: { defaultAppIcon: false },   // 不合成默认图标
});
```

生成内核现在收在一个包里：`@opentray/icon`。字形默认值、合成与 squircle 切图都在其中，ICNS、ICO 和 Linux PNG 三档编码也一并收进来，底层是一套 WebAssembly 图像栈（jsquash 负责解码与缩放，resvg 负责渲染 SVG），全程没有 sharp，也没有 libvips。`@opentray/vite-plugin` 调的是同一个内核，公开 API 不变。

向导的 URL 模式把 macOS 应用需要的两个图标分开处理。App 图标候选是原图加上 AI 抠出的主体；托盘图标用实心剪影，后者才是 macOS 托盘模板要的东西。抠图在浏览器里跑，模型由向导服务端代理，后面垫着一层持久的磁盘缓存：模型从 CDN 下载一次，之后每次会话（端口随机）都从本机回环地址取。高级设置（模型精度、alpha 阈值与边缘收缩）改动即生效，替换掉上一个主体和由它派生的剪影。

## 其他变更

- macOS 上的 `@opentray/ext-webview` 不再在每个事件排空周期重复断言激活策略。原来每秒约 60 次的 tick 都调 `set_activation_policy`，连带重设应用图标并重绘 Dock 图块，一个 broker 稳定吃掉单核 50–80%，哪怕窗口内容一动不动。现在先跟 AppKit 的当前值比对再决定（[029d286](https://github.com/jixoai/opentray/commit/029d286e01e933e9961de116ec69428740662ac8)）。
- `@opentray/icon` 的自动背景会匹配实色边缘环，白底方角 favicon 能保住自己的底色。
- 需要中日韩字符但系统没装对应字体时，图标生成退到中性终端标记，不再直接失败。
- `@opentray/vite-plugin` 去掉了 sharp 依赖。

## 升级

```bash
npm i create-opentray@0.23.0
npm i opentray@0.23.0          # SDK
```

用官方扩展时锁同一条协议线：`pnpm add opentray@stable-A-B @opentray/ext-webview@stable-A-B`（协议线 tag 由 `@opentray/spec` 发布，不要和 `latest` 混用）。

## 链接

- Changelog：[GitHub Release opentray@0.23.0](https://github.com/jixoai/opentray/releases/tag/opentray%400.23.0) · [opentray@0.22.0](https://github.com/jixoai/opentray/releases/tag/opentray%400.22.0)（图标内核那一条，同日更早发布）· [packages/create/CHANGELOG.md](https://github.com/jixoai/opentray/blob/main/packages/create/CHANGELOG.md)
- 文档：[opentray.jixoai.com](https://opentray.jixoai.com) · [create README](https://github.com/jixoai/opentray/blob/main/packages/create/README.md) · [app-mode 指南](https://github.com/jixoai/opentray/blob/main/skills/opentray/references/app-mode.md)
- 升级：见上面的升级节
- 反馈：[GitHub Issues](https://github.com/jixoai/opentray/issues)
- npm：[opentray](https://www.npmjs.com/package/opentray) · [create-opentray](https://www.npmjs.com/package/create-opentray)
- 本站：[OpenTray v0.21.1](/zh/blog/2026-09-06-opentray-v0-21-1/) · [UniPty v0.2.2](/zh/blog/2026-09-07-unipty-v0-2-2/)
- English version: [/blog/2026-09-10-opentray-v0-23-0/](/blog/2026-09-10-opentray-v0-23-0/)
